package controller;

import domain.Menus;
import domain.Tables;

public class TerminateMachine implements PosMachine {
    @Override
    public void execute(Tables tables, Menus menus) {
        return;
    }
}
